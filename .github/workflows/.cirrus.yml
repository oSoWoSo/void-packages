persistent_worker:
  isolation:
    container:
      image: debian:latest
      cpu: 24
      memory: 128G
      volumes:
        - /:/test
        - /tmp/persistent-cache:/tmp/cache:ro

task:
  script: uname -a
